import Guest from "@/components/dashboard/home/guest";
import React from "react";

const page = () => {
    return (
        <div>
            <Guest></Guest>
        </div>
    );
};

export default page;
